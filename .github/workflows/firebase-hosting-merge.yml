# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          node-version: 'latest'

       # 🔹 Clona el repositorio
      - uses: actions/checkout@v4

      # 🔹 Instala la última versión de Node.js
      - uses: actions/setup-node@v4
        with:
          node-version: 'latest'

      # 🔹 Instala dependencias
      - run: npm ci

      # 🔹 Instala Angular CLI globalmente
      - run: npm install -g @angular/cli

      # 🔹 Verifica versiones instaladas (opcional)
      - run: node -v && ng version

      # 🔹 Compila el proyecto
      - run: ng build --configuration=production

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_GESTOR_TAREAS_ANGULAR_WEB }}
          channelId: live
          projectId: gestor-tareas-angular-web
